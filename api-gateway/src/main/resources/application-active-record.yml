
spring:
  cloud:
    gateway:
      routes:
        - id: account-service
          uri: lb://account-service/
          predicates:
            - Path=/account/**
          filters:
#            - TokenRelay=
            - RemoveRequestHeader=Cookie
